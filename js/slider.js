var slideActive = 0;var slideSpeed = 300;function slideInit(){    var slides = $('#contentSlider .ui-slide');    var url = "";    var active = '';    var activeSlide = 0;	    if (slides.length > 0)    {        url = new String(window.location);		        if (url.indexOf("#", 0) != -1)        {            active = url.split("#")[1];        }		        slides.each(function(index, element){            $(element).css('display', 'none');        });		        activeSlide = $(slides).first();        if (active != '')        {            if ($('#' + active + '.ui-slide').length == 1)                activeSlide = $('#' + active + '.ui-slide');            else                activeSlide = $(slides).first();	        }		        $(activeSlide).css('display', 'block');        $(activeSlide).toggleClass('ui-slide-active');        $(activeSlide).removeClass('ui-slide');    }}function slideLeft(){    var active = $('.ui-slide-active');    var prev = $(active).prev('.ui-slide');	    if (prev.length == 0)    {        prev = $('.ui-slide').last();    }	    if (!slideActive)    {        slideActive = 1;		        $(active).toggleClass('ui-slide');        $(prev).toggleClass('ui-slide-active');		        $(active).removeClass('ui-slide-active');        $(prev).removeClass('ui-slide');		        $(active).hide('drop', {            direction: "right"        }, slideSpeed);        $(prev).show('drop', {            direction: "left"        }, slideSpeed, function(){            slideActive = 0;        });		        window.location = '#' + $(prev).attr('id');    }}function slideRight(){    var active = $('.ui-slide-active');    var next = $(active).next('.ui-slide');	    if (next.length == 0)    {        next = $('.ui-slide').first();    }	    if (!slideActive)    {        slideActive = 1;		        $(active).toggleClass('ui-slide');        $(next).toggleClass('ui-slide-active');		        $(active).removeClass('ui-slide-active');        $(next).removeClass('ui-slide');		        $(active).hide('drop', {            direction: "left"        }, slideSpeed);        $(next).show('drop', {            direction: "right"        }, slideSpeed, function(){            slideActive = 0;        });		        window.location = '#' + $(next).attr('id');    }}